<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Register Page</title>
  <link href="./css/site.css" type="text/css" rel="stylesheet">
  <link rel='shortcut icon' type='image/x-icon' href='./images/favicon.png' />
</head>

<body>
  <div class="container">
    {{!-- header  --}}
    {{> header}}
    <main>
	<h1>Register Form</h1>
<div class="form">
    <form action='/register' method="POST">
        <label for="username">Username</label>
        <input type="text" id="username" name="username">
        {{#if errors.username}}
          <div class="error">{{ errors.username.message }}</div>
        {{/if}}
      
        <label for="password">Password</label>
        <input type="password" id="password" name="password">
        {{#if errors.password }}
          <div class="error">{{ errors.password.message }}</div>
        {{/if}}

        <label for="repeatPassword">Re-Password</label>
        <input type="password" id="repeatPassword" name="repeatPassword">
        {{#if errors.repeatPassword}}
          <div class="error">{{ errors.repeatPassword }}</div>
        {{/if}}
        <input type="submit" value="Register">
    </form>
</div>
    </main>
  </div>
  <footer>@Cubicle: Exercise for Express.js and Handlebars</footer>

</body>

</html>